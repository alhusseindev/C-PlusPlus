#include <cassert>
#include <iostream>
using namespace std;
int main()
{
    double x = 0.0;
    cout << "Enter a number:" << endl;
    cin >> x;

    // Uncomment the line that is the correct answer!
    //
    // We want to print out the square of a number if it less than 1,
    // which line of code aproperly checks this?
    // --------------------------------------------------

    if (x < 1.0)

    // if (x >= 1.0)

    // if (sqrt(x) <= 1.0)

    // if (x)

    // --------------------------------------------------
    {
        cout << x * x << endl;
    }
}
