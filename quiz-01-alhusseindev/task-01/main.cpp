#include <iostream>
using namespace std;
int main()
{
    int a = 0;

    // Uncomment the line that is the correct answer!
    //
    // Which of the following properly checks if the integer a is equal to 0?
    // --------------------------------------------------
    // if ((a = 0))
    
    // if (a equals 0)

    // if (a is 0)

    if (a == 0)

    // --------------------------------------------------
    {
        cout << "Hello World!" << endl;
    }
}
