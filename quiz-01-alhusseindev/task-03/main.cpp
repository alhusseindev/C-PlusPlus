#include <cassert>
#include <iostream>
using namespace std;
int main()
{
    int x = 0;
    cout << "Enter a number:" << endl;
    cin >> x;

    // Uncomment the line that is the correct answer!
    //
    // We want to print out the the counter variable x so long as it
    // is less than 100. Uncomment the line of code that would achieve
    // this.
    // --------------------------------------------------

    // while (x > 100)

    // if (x < 100)

    while (x < 100)

    // while (false)

    // --------------------------------------------------
    {
        cout << x << endl;
        x++;
    }
}
