#include <iostream>
using namespace std;

int main()
{
    double *p = nullptr;

    // 1 point
    //
    // Select the conditional that will pass if p is
    // a null pointer.
    //
    // ------------------------------------------------

    // if (p != nullptr)

    if (p)

    // if (p == *p)

    // if (!p)

    // ------------------------------------------------
    {
        cout << p << endl;
    }
}
