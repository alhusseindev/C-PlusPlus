#include <iostream>
using namespace std;
int main()
{
    int a = 1337;

    // 2 points
    //
    // Uncomment the line that is the correct answer!
    //
    // Which of the following pairs statements properly
    // changes the value of a by multiplying it by 10, and
    // then adding 11 to it?
    //
    // Make sure you uncomment both lines in a single pair!
    // --------------------------------------------------

    a *= 10;
    a += 11;

    // a * 10;
    // a + 11;

    // a + 11;
    // a * 11;

    // a *= 11;
    // a *= 10;

    // --------------------------------------------------
    cout << a << endl;
}
